#include <stdio.h>

int main(int argc, char *argv[]) {
    /*
     * Print the value of EOF (-1, you can see it if you go to definition in the
     * stdio.h file)*/
    printf("%d", EOF);
    return 0;
}
